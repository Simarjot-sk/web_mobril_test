name: web_mobril_test
description: Flutter machine test for web mobril technologies
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_sign_in: ^6.1.4
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.0
  firebase_core: ^2.14.0
  flutter_svg: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/